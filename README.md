# TimeBlock iOS

A SwiftUI-based rewrite of TimeBlock for iOS.

## Features
- Display a list of tasks with durations
- Add and remove tasks

## Building
This project uses Swift Package Manager. Open the package in Xcode on macOS to build and run the app. On Linux, `swift build` will fail because SwiftUI is unavailable.
